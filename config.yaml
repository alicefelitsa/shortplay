# ===========================
# 服务器端口配置
# ===========================
server:
  # Web服务端口
  webPort: 8100
  # 限流队列容量
  queueCapacity: 1000
  # 限流突发容量
  suddenCapacity: 2000

# ===========================
# MySQL数据库配置
# ===========================
mysql:
  # 数据库地址
  address: "2.28.7.100:3306"
  # 数据库名称
  database: "drama"
  # 数据库用户
  user: "drama"
  # 数据库密码
  password: "M2dPKXCpdzDdzs6z"

# ===========================
# Redis配置
# ===========================
redis:
  # Redis服务器地址
  address: "localhost:6379"
  # 密码，没有则为空
  password: ""
  # 默认数据库
  db: 0
